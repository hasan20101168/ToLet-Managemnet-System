<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">

    <!-- Brand -->
    <a class="navbar-brand" href="/rentals">🏠 To-Let System</a>

    <!-- Toggle (mobile) -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Menu -->
    <div class="collapse navbar-collapse" id="navbarNav">

      <ul class="navbar-nav ms-auto align-items-center">

        <% if (currentUser) { %>

          <!-- Role-based links -->
          <% if (currentUser.role === "owner") { %>
            <li class="nav-item">
              <a class="nav-link" href="/rentals/owner/dashboard">Dashboard</a>
            </li>
            <!-- <li class="nav-item">
              <a class="nav-link" href="/rentals/new">Add Rental</a>
            </li> -->
          <% } %>

          <% if (currentUser.role === "tenant") { %>
            <li class="nav-item">
              <a class="nav-link" href="/rentals">Browse Rentals</a>
            </li>
          <% } %>

          <% if (currentUser.role === "admin") { %>
            <li class="nav-item">
              <a class="nav-link" href="/admin/dashboard">Admin Panel</a>
            </li>
          <% } %>

          <!-- <% if (currentUser && currentUser.role === "owner") { %>

            <a href="/messages/owner/inbox" class="nav-link position-relative">

              <i class="bi bi-chat-dots-fill"></i>

              Messages

            </a>

          <% } %> -->

          <!-- User Info -->
          <li class="nav-item">
            <span class="nav-link text-light">
              👤 <%= currentUser.name || currentUser.email %>
            </span>
          </li>

          <!-- Logout -->
          <li class="nav-item">
            <form action="/logout" method="POST" class="d-inline">
            <button class="btn btn-sm btn-danger ms-2">
                Logout
            </button>
            </form>
          </li>
          
          

        <% } else { %>

          <!-- Not logged in -->
          <li class="nav-item">
            <a class="nav-link" href="/login">Login</a>
          </li>

        <% } %>

      </ul>

    </div>
  </div>
</nav>